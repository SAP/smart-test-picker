# EI-16 findings ledger

Created from STP `9881141` and Jenkins plugin `ef91832` before EI-16 edits; finalized after production-path and packaged-runtime verification.

| finding | applicable contract | production classes/methods | existing test evidence | reproduction result | required fix | final verification | disposition |
|---|---|---|---|---|---|---|---|
| Failed tests retaining partial coverage | Failed declarations belong in `unmapped/FAILED`; partial edges are unsafe exclusion evidence. ABORTED-only/disabled declarations are non-executed. | `AsmCoverageFragmentProjector`; `JacocoPerTestListener`; Maven/Gradle JaCoCo fragment generators; qualification/join; `ExplicitPrSelectorFlow`; selector | EI-14 proves ABORTED/reset isolation, not failure before an unreached dependency. | CONFIRMED: ASM and both JaCoCo projections mapped `FAIL` with partial edges. | Failure-dominant `unmapped/FAILED`; discard partial mapping; preserve complete/order-independent accounting. | Runnable Gradle ASM and Maven JaCoCo fixtures call A, fail before B, retain failure XML, contain no B edge, and emit only FAILED; production selector selects the identity when B changes. Full suite passes. | **FIXED** |
| Setup scopes without execution-target ownership | Every schema-v3 setup relationship retains one executable target; same-container scopes from different targets do not collide. | `SetupScope.owner`; JaCoCo listener/Maven projector; qualifier; executable codecs/validator/join; Gradle/Maven aggregators; selector projection | EI-15 single-target setup evidence did not prove duplicate binary containers across targets; Maven emitted no setup scopes. | CONFIRMED: qualification preserved unowned IDs, joins keyed only by ID, and Maven silently missed bounded setup. | Require owner; key by owner+ID; capture Maven bounded class-container setup; reject ownerless v3 setup payloads. | Two Gradle Test tasks and two Maven modules use `SharedTest` with setup-only A/B dependencies; owners/dependencies survive codec and join. Ownerless wire payload is rejected. | **FIXED** |
| Schema-v3 executable-to-logical projection | Logical union is valid if exact executable inventory ownership is retained for routing and adapters execute the complete safe set. | `ExplicitPrSelectorFlow.selectionMap`; CLI expansion; Jenkins `SelectorBridge`, runner, selection step and adapter | EI-15 single-target workflow; earlier CLI test had two owners but identical coverage. | Changing only ProductionA selects the logical identity and expands it to both exact executable owners. | No precision redesign; strengthen packaged production oracle. | Docker Jenkins expected = selected = executed for module A and B canonical identities; both `notSelected` methods excluded. | **VERIFIED CORRECT — CONSERVATIVE** |
