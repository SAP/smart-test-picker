# G04 experiment plan

Question: do the two annotation-test bridge paths share G03's reflection-order trigger?

The 20 edges form two structurally similar ten-method paths and share `[1,0,1,1,1]`. Reuse ROUND 11's rejection of cross-test BridgeMethodResolver cache reuse for the selected miss-path representatives and ROUND 13's rejection of cache hit/miss for the selected moving edge. Search retained ROUND 14-15 evidence for a producer-order trace connected to either G04 test.

No such target-connected trace exists. G03's raw producer observation concerns a different test and queried declaring class. Equal vectors and shared consumers justify the analysis group but cannot establish the same producer or decision. A fresh targeted reflection trace for both tests would be the next evidence needed; extending diagnostics and sampling additional rare present/absent runs is beyond this final bounded pass.

Result: `INSUFFICIENT_EVIDENCE`; all 20 edges remain `UNKNOWN`.
