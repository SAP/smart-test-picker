# Historical TASK 24 schema-v1 evidence

These compact files preserve the intermediate TASK 24 projection result before descriptor-aware
schema v2 was introduced. They are historical evidence, not current contract fixtures, and must not
be rewritten to look as though TASK 24 originally emitted schema v2.

Current regression authority is the schema-v2 test coverage in `smart-test-picker-common`,
`stp-runtime`, `stp-junit-adapter`, and `stp-agent`.
