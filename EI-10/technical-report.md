# EI-10 technical report

EI-10 completed the real Docker Jenkins chain on Spring Framework R0 `a099debca9e59eb562cb3298690423dbb9d0127e` and local R1 `a11311c52a72cf1a6cef50f2942ffb79dfc4a69d`.

The Gradle plugin now has assignment-free schema-v3 executable discovery scoped to canonical Test task paths. Discovery and worker mapping share strict target resolution; discovery never requires an assignment or starts coverage. Gradle 9 project-lock compatibility, class-cohesive sharding, recursive abstract-container exclusion, and positive skipped-test evidence accounting were validated in the real Spring Core run.

Job A build 14 used three physical mapping containers concurrently and a fourth physical join container. The authoritative inventory contained 4,127 executable identities. Assignment sizes were 1,488, 1,392, and 1,247. All began at `12:51:15Z`; their finish times were `12:53:42Z`, `12:53:48Z`, and `12:53:52Z`, proving overlap. The join produced 4,123 mapped entries; four assigned identities had positive `NON_EXECUTED` evidence. The immutable published file SHA-256 is `f5e875b33d550fb8dda1d68b416719c3a3b4ca0d9b9c8bd42886ee2929ab4749`.

The deterministic seed was 1010. After sorting and filtering, 299 safe concrete mapped production classes remained. The first eligible shuffled three-class combination was `PathMatchingResourcePatternResolver`, `DefaultPropertySourceFactory`, and `MapToMapConverter`; no earlier combination was rejected. Exactly one diagnostic print was added to one concrete method in each class.

The independent class-level calculation includes direct test coverage and mandatory setup-scope propagation. It expected 228 of 4,127 inventory identities (5.5246%). Job B build 5 resolved the FILE-backed R0 map, generated the real R1 inventory, ran `stpExplicitPrSelect`, passed its SELECT plan to `smartTestPicker`, and produced exactly the same 228 identities in Gradle XML. The separate R1 full suite passed with 4,317 executed cases and contains every selected test.

No inventory, assignment, fragment, map, selector result, or Gradle test filter was manually supplied in the accepted E2E. Failed builds were retained only as diagnostic evidence and never supplied acceptance artifacts.
